/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
    basePath: "/phs-alumni-site",
    output: "export",
    reactStrictMode: true,
};
export default nextConfig;
