/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
    basePath: "/smcs-alumni",
    output: "export",
    reactStrictMode: true,
};
export default nextConfig;
